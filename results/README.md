# Results

Generated result bundles can be written here with:

```bash
pnpm benchmark publish-result --run runs/public-search-200 --out results/public-search-200
```

Each bundle contains `raw.jsonl`, `result.json`, `manifest.json`, and `checksums.txt`.
