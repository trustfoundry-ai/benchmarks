// Adapter contract barrel. See ./README.md.
// Phase 3 of the refactor introduces `defineProviderAdapter`,
// `defineBenchmarkAdapter`, and `defineScorerAdapter` factory helpers.
// For Phase 0 the type declarations in the sibling `.d.mts` files are the
// contract; this file exists so external consumers can `import '@trustfoundry-ai/benchmarks/contracts'`
// once the package exports map is wired in Phase 3.
export {};
